@model KMGames.Web.Models.Cart.Cart


<form class="d-flex" action=@Url.Action("Index","Cart")>
    <button class="btn btn-outline-light ml-5" type="submit">
        <i class="bi-cart-fill me-1 fa fa-shopping-cart"></i>
        <span class="badge bg-dark text-white ms-1 rounded-pill">@Model.Count()</span>
    </button>
</form>
